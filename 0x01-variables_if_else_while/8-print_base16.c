#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: print all ther numbers of bass 16
 * in lowercase followed by a new line
 * Return: 0
 */
int main(void)
{
	char miles;

	for (miles = '0'; miles <= '9' miles++)
	{
		putchar(miles);
	}
	for (miles = 'a'; miles <= 'f'; miles++)
	{
		putchar(miles);
	}
	putchar('\n');
	return (0);
}
